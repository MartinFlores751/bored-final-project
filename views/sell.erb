<div class="container" >
  <h1>Here's the charts you have uploaded!</h1>
  <% if @s.empty? %>
  <p>No sheets under your account, please upload a new sheet</p>
  <% else %>
	  <% @s.each do |s| %>
	    <div class="card text-white bg-secondary mb-3" style="max-width: 20rem;">	
	      <img class="card-img-top" src=”img/<%= s.file_path %>”>	
	      <div class="card-header"><%= s.title %></div>
	      <div class="card-body">
	        <h5 class="card-title"><%= s.description %></h5>
	        <p class="card-text"><%= s.created_at.to_s %></p>
	      </div>
	    </div>
	  <% end %>
  <% end %> 	  
</div>
